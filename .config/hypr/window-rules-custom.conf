# Правила окон, управляемые window-default.sh.
# Каждая запись привязана к классу окна и перезаписывается при повторной настройке того же класса.
windowrule = match:class ^(ru\.wayzoapp\.src)$, float yes, size 400 800
windowrule = match:class ^(com\.savetime\.app\.save_time)$, float yes, size 400 800
windowrule = match:class ^(anki)$, float yes, size 800 450
windowrule = match:class ^(md\.obsidian\.Obsidian)$, float yes, size 1600 900
